{
  "name": "rag-benchmark-lab",
  "private": true,
  "scripts": {
    "setup": "python -m pip install -r requirements.txt && npm install --prefix frontend",
    "backend": "python -m uvicorn api.main:app --reload --port 8000",
    "frontend": "npm --prefix frontend run dev"
  }
}
